# Optional — only needed for server-side OG inference
# Most users should connect their wallet in the browser instead
# OG_PRIVATE_KEY=0x...

# MemSync credentials (optional — demo mode works without)
# MEMSYNC_API_KEY=your_memsync_api_key_here

# Server config
HOST=0.0.0.0
PORT=8000
DEBUG=true
